# CLAUDE.md — The Trailprint Atlas

Working guide for Claude (and any future collaborator) on this project. Its companion is
[docs/STATE_OF_THE_ATLAS.md](docs/STATE_OF_THE_ATLAS.md) — the living plan of record (the
July 2026 revision; the original six-phase audit lives in git history). Read that for
*where we're going*; read this for *how to work here*.

## What this is

The Trailprint Atlas is a living journal of Danny's hiking life — a personal memoir told
through maps, data, and (eventually) his own words. The audience is friends and family, not
the public. It is intentionally **not** a generic hiking app: every design choice should make
it feel more like *his* atlas.

Guiding principles (inherited from the project's first charter, still true):
- **Data as narrative** — don't just show numbers; make them tell the story of a day outside.
- **Visual first** — map-centric, natural palettes, clean and immersive.
- **Simplicity & power** — vanilla web tech, no frameworks, no build step. Craft over tooling.
- **Personal & authentic** — the voice belongs to Danny. See "The voice rule" below.

## How we work together (read this first)

Danny is a non-coder ("vibecoder at best"). Claude is his hands-on technical partner.

1. **Explain everything in plain English as you go** — what you're doing and why.
2. **Small visible steps.** He watches changes land live via the VS Code Live Server
   extension. After each meaningful change: say what changed, where to look in the browser,
   and **wait for his OK** before moving on. Never one giant batch.
3. **Decisions in Part V of the roadmap are his.** Ask one at a time, at the moment the
   decision actually blocks work, with a recommendation and plain-English reasoning.
   Decided so far (June 2026): "Miles Hiked" counts **every hike including repeats**;
   the nav link to the latest hike was labeled **"Logbook"** — and was then
   **removed entirely in July 2026**: a hike is reached through the land (the
   interactive map, or a trip chapter opened from it), never by jumping straight
   to whichever hike happens to be newest. `timeline-nav.js`/`.css` went with it.
4. **Desktop first.** All mobile-specific work is deferred to its own future phase. Don't
   "fix" mobile things in passing. Performance work (payload size, load speed) is in scope —
   it benefits everyone.
5. **Git: no feature branches, and never commit before he approves.** Solo project. Finish
   a chunk, then hand him **specific things to check and play with in the browser** — which
   page, which element, what to try — and wait. Approving one chunk is not standing
   permission for the next; ask every time. Once he says yes, commit straight to `main` and
   push (GitHub Pages deploys immediately). Keep commits small and verified; the site should
   work at every commit.

### The voice rule

The `notes` field, The Overlook personal statement, and the loading phrases are **Danny's
words only** — draft nothing there without his raw material, and only ever edit, never invent.
The `description`/`flora`/`fauna` fields are the AI-drafted "field guide" layer; drafting
there is fine, with his review.

## Stack (the real one)

- Static site: HTML + CSS + vanilla JS (ES6+). **No build step, no bundler, no framework.**
- [Leaflet 1.9.4](https://leafletjs.com) + leaflet-gpx 1.7.0, pinned from CDNs in each page's HTML.
- **Cloudinary** (cloud name `dgdniwosl`) serves all photos via URL transforms
  (`w_800,h_600,c_fill,q_auto,f_auto/...`). Free tier.
- **Open-Meteo archive API** powers the Hike Almanac (historical weather + sunrise/sunset).
- Hosted on **GitHub Pages**. Total cost: $0/month. This architecture is a deliberate,
  audited choice — renovate it, don't replace it.
- **Local dev:** must be served over HTTP (fetching local `.json`/`.gpx` fails from
  `file://`). Danny uses the Live Server VS Code extension.

## File map

| Path | What it is |
|---|---|
| `index.html` | Homepage shell — **THE KEY MAP** (August 2026). The hero film runs first, unchanged; below it the page is a **volume lying open** (`.volume-board > .volume > .spread`): the **left leaf** is a satellite detail plate that never roams (`scripts/keymap.js`), the **right leaf** (`.matter`) is four plates of front matter — I The Abstract (the Odometer, `home.js`), II Threads of the Trail (`threads.js`), III The Observatory (`observatory.js`), IV The Record Books (`records.js`). Each wears a `.p-collar`; the old four coloured bands are gone. **Every plate cross-lights the leaf** — see "The cross-lit spread" below. Sections keep their original class names (`odo-section`, `threads-section`, …) because their `.in` reveal classes depend on them. A small inline sessionStorage/reduced-motion check stays inline by design (must run before first paint — it fast-forwards the hero film for repeat visits and reduced-motion visitors). **Leaflet IS loaded here again** (August 2026) — the detail plate is a Leaflet map. It was removed in July when the SVG film that used it for projection was replaced by MapLibre; the film is still MapLibre and still the only thing on the page that uses it, so the page knowingly carries both libraries. `trail-renderer.js` is still not loaded. |
| `404.html` | Branded "Off the Trail" page (GitHub Pages serves it for bad URLs). Self-contained on purpose — no relative assets. |
| `scripts/config.js` | `ATLAS_CONFIG` (Cloudinary cloud + `cloudinaryUrl()`, year colors, type icons, `STAMPS` — the hand-drawn hike-type SVG glyphs rendered via `atlasStampSvg()`, seasons) + `blurUpShow()`/`blurUpPreload()` — the lightbox photo loader: hover warming + after-landing neighbor preloads make flips land instantly; the current frame simply holds while a cold photo travels. **No placeholder states by decision** (a holding card was tried and retired July 2026 — every variant flickered; don't reintroduce one). Also `TRIP_STARS` + `tripStar()` — **the curated opening frame for each trip**, `trip_tag` → Cloudinary public ID. Hand-picked by Danny, never derived: a guessed frame put a selfie or a car park on the grandest surface in the Atlas. Pick new ones with `mockups/trip-star-picker.html`; an unlisted trip falls back to its most-photographed stop's first frame, which is a placeholder, not a choice. **The wizard asks for one whenever a hike joins a trip that hasn't got a star yet** (August 2026) and writes it here — a hand-picked list only stays complete if the picking happens when the trip is created. And `tripName()` — the one place a `trip_tag` is split into its display name (three copies of that regex used to drift around the codebase). Load before all other Atlas scripts. |
| `scripts/atlas-data.js` | Data layer: cached `fetchHikes()`/`fetchTrailGeometries()`, `groupByTrail`/`groupByTrip`, `hikeYear`/`hikeMonth`, `formatHikeDate`, `compareHikesChrono`/`Desc`, `getAtlasStats`. |
| `scripts/nav.js` | Injects the single shared nav structure (synchronously) + the site footer on every page. |
| `scripts/home.js` | The homepage's **intro coordination** (`AtlasIntro` — one `skip()` cancels every pending timer and lands the film and the nav together), the Odometer stats reels, and the nav loading-bar phrases (Danny's words). It must load **before** `intro-film.js`, which hangs off `AtlasIntro`. Note `AtlasIntro` is declared with `const`, so it is a global *binding* and **not** a property of `window` — `window.AtlasIntro` is undefined even here, and code that tested it that way silently did nothing (it left the nav's loading phrases running after a skip). Reference it bare, behind a `typeof` guard. |
| `scripts/intro-film.js` | **The Life in Trails** — the opening film, and the single copy of its choreography. Two halves joined by a cloud: the first is a **baked video** (a 3D MapLibre-terrain flight over the San Gabriels, the PCT leg inking itself, the Angeles forest lighting up around it); the second is a **live MapLibre map** — the Atlas sheet pulling back to the whole country while the rest of the trailprints draw themselves in. Flying it live costs ~30 MB and 2,200 requests; baked it is one 5.4–14.8 MB video, and the live half means **a hike added tomorrow appears with no re-render**. **PLAYBACK SMOOTHNESS RESTS ON TWO RULES, BOTH LEARNED THE HARD WAY (July 2026).** First, **the codec is pinned to h.264** (`f_mp4,vc_h264`), never `f_auto:video`: f_auto sends VP9 to Chrome and Firefox, and **Apple Silicon has no VP9 decoder** — asked of VideoToolbox on an M3, h.264 and HEVC are hardware and VP9 is software — so every Mac visitor outside Safari was decoding 1080p on the CPU and the film juddered. h.264 costs bytes (1920: 9.4 MB vs VP9's 6.8) and buys a decode that cannot fall behind. Don't "modernise" it to AV1 either: that same M3 reports AV1 hardware and VP9 software, the reverse of what the codecs' ages suggest, and `mediaCapabilities.decodingInfo()` is no help — it answers `powerEfficient: true` for everything, including AV1 at 2560 on hardware with no AV1 decoder. Second, **the video is the clock, and nothing ever seeks it while it plays** (`filmQ`): `q` used to come from the wall clock with `syncFilm` correcting the video whenever it drifted 80 ms, and every correction is a decoder flush that puts the video *further* behind, so a machine slightly late became one that juddered. Now the ink, cloud and camera derive from whatever the decoder actually showed, so the two cannot disagree; a slow machine plays a slightly slow film in perfect step with itself. The wall clock carries `q` between the video's 30fps ticks (capped at one frame of lead) so the live layers still run at full rate, and a picture that stops for `STALL_MS` hands the clock back re-anchored, so a film that never arrives still lands. Width comes from the layout, **never from `navigator.connection.downlink`** — read in the page's first milliseconds it is a conservative guess, so Chrome demoted itself to 1280 at random while Firefox, which doesn't implement `navigator.connection` at all, could never demote and always pulled the largest cut. **ONE FILM, TWO HOSTS:** `index.html` and `mockups/option-c-3d-cinematic.html` (the cutting room) load this same file and `styles/home.css`; it builds its own layers inside `#hero-film`, adopts whatever chrome the page already has, and publishes `window.AtlasFilm` (plus `AtlasFilmReady`, since it boots async). Never fork it. The **vignette is the film's own** (`.if-vig`), not the page's — it is baked into the video, and wearing the homepage's lighter one lifted the corners at the seam (1.01 → 2.19 /255). **The handover is a CUT, not a crossfade** (`liveShare`): fading the video out while the live cloud faded in put a 50% video over a 50% cloud, and two half-opacity whites do not add up to one opaque one — the map flashed into view mid-swap. It is safe only because S4 sits inside the cloud's full white hold. Trails are gated on their **whole footprint** (bbox corners), never a centroid — a 9 km trail's midpoint can be in frame while a third of the line is off the top. `?q=`/`?p=` park the film at a moment for screenshots. |
| `scripts/keymap.js` | **The left leaf** — the only file on the home page that knows Leaflet exists; every other section speaks to `window.AtlasKeyMap` (`ready`/`light`/`clear`/`plate`/`benchmarks`/`showBenchmarks`). One big **detail plate** (Esri World Imagery + Esri reference labels) and one small engraved **index diagram**. **The camera never moves on its own**: it CUTS between subjects behind a parchment veil (map.html's device, 130 ms down, 190 ms floor, 1.4 s ceiling, gated on `waitTiles`) and never flies; only a layout event may re-frame it. **`zoomSnap: 1` is not a preference** — at a fractional zoom Leaflet scales the tile grid onto fractional pixels, the edges antialias, and the plate's backing prints a white lattice across the imagery; a dark `#22261f` backing makes any residue read as shadow. **`clear()` is deliberately a no-op**: the plate holds the last subject asked for, because a reader running an eye across the Effort Field was watching the leaf slam in and out of California between every hike. The index diagram is **not a map** and costs no tiles — it replays the LAEA projection from `assets/atlas-frame.json` to plant a dot per hike. |
| `styles/keymap.css` | The volume, the spread, the leaf and the shared **plate collar** (`.plate`/`.p-collar`/`.p-lede`) used by all four front-matter plates. The binding gradient sits at 43.4–44.6% because that is where the 0.88fr/1.12fr columns actually divide; `.spread` must be `align-items: stretch` or the sticky leaf rides ~700 px and then scrolls away. `--leaf-chrome` is the measured height of everything around the plate — head rule, collar, door, paddings, sticky offset — and the plate takes whatever the viewport has left. |
| `assets/atlas-frame.json` | **Generated — don't hand-edit.** The index diagram: every country the Atlas has walked, projected through ONE Lambert azimuthal equal-area centred on their combined middle, plus that projection's own parameters so a hike's lat/lon can be re-projected in the browser and land where it belongs. Written by `tools/build-countries.py`. |
| `scripts/threads.js` | **Threads of the Trail** (Plate II): the **milestone ledger**, computed live from hikes.json. The procedural USGS quadrangle it used to draw was retired August 2026 — invented contours cannot share a spread with real ground photographed from orbit, and the benchmarks now stand at each milestone's true coordinates on the index diagram. The engine, glyphs and notes are untouched. **The drawer is opened by a CLICK, and this is the third mechanism tried** — scroll-driven and hover-driven both failed for the same structural reason: a drawer lives between its entry and the next, so opening one MOVES every entry below it, and the thing choosing which to open was the thing being moved. The file header has the long version. There is **one** drawer element, it spans the ledger's full width, and it is re-seated under whichever grid ROW was opened (in two columns it must not belong to a column). The clicked row is held to the pixel by measuring its position before and after every DOM change; any in-flight smooth scroll is cancelled first, or the two measurements describe different pages. Two columns past 1900 px, read left-to-right then down, with engraved line numbers so the sequence survives the layout. |
| `scripts/observatory.js` | **The Observatory** (Plate III): profile line, the Effort Field scatter, **Territories** (a mosaic — a territory occupies a block of the index grid sized by outings, 3×3 / 2×2 / 1×1; the column count is chosen so the biggest block always reads as the same fraction of the sheet, and the shortfall is printed as blank index squares so the block is always a full rectangle), **The True Ascents** (summit climbs as their real elevation profiles from `data/elevations.json`), **The Cadence** year-wheel, **The Triangulation Network** (companions as survey stations, every pair who've walked together joined by a baseline weighted by outings shared; angle is the middle of a station's era, distance in from the rim is company kept, and the primary station at the centre is Danny — the design is documented in the code, the mockup it came from was exploratory and stayed local), and **The Specimen Drawer** (biome cabinet; hand-picked photos via `SPECIMEN_PICKS` — the section's one deliberate photo exception). **Every panel answers on the left leaf** via `lightLand()`; the shared floating tooltip `#obs-tip` is **gone** (August 2026) and must not come back. Wide charts are **re-drawn** to the measured width, never scaled to it (R2). |
| `scripts/records.js` | **The Record Books** (Plate IV): the standing crowns → hike pages, and the expedition podium → trip chapters. Every photograph is in a **paper mount** — window above, engraved caption printed on the paper below — never a scrim with white type over it. **Four mounts, four different trails, and every figure true:** one day can top two boards (tta_47 is both the longest hike and the biggest climb), and the tidy fix of letting a crowned trail step aside for the next best is FALSE, so a trail holding two records wears both and the board fills back to four from an ordered reserve. Podium mounts wear the **trip star**; crowns deliberately do not, because a record moves whenever one is beaten and a curated list that must be updated every time is a system built to be forgotten. |
| `map.html` + `scripts/map.js` | Interactive map (rebuilt July 2026): full-bleed map, floating Atlas-crafted cards, and the **expedition engine** — Chapters of the Land + the Expedition Line. **The camera never moves on screen: it is always parked on a scene or cutting behind the veil**, which lifts only when the tile sheets report loaded (live multi-zoom camera flights were tried three ways and rejected — never reintroduce them). All motion belongs to the ink: trails draw themselves along their GPX; a dashed journey line travels trailhead-to-trailhead between chapters. Structure (airtight, data-driven): **chapter** = a tagged trip that truly left home / a home stretch / a lone far-away day hike ("sortie"); distance overrides trip tags (local overnights stay home, `SORTIE_KM = 73`); **scene** = consecutive legs sharing one `location` tag; the camera parks per **shot** (scene legs merge only while their union still frames at z12 or closer — lone stops get the tightest frame); scene/shot changes are textless veil blinks, chapter changes get one combined ceremony slide. **Time-aware ink**: every VISIT owns its own stroke — the newest visible visit rides solid in its year's color, earlier visits fade to echo whispers, markers and the gold repeat ring follow `nowT` (never render a trail in a future year's color). Trailheads wear **stamps**, not pins (July 2026): a small year-ink dot at the exact point + the type's engraved SVG glyph (`ATLAS_CONFIG.STAMPS`) seated beside it, offset away from the trail's own ink (`stampVector` — opposite the bisector of the opening/closing bearings) and zoom-gated: below `ICON_REVEAL_ZOOM` the stamps fold into their dots. Gold repeats = ring on the dot + gold-leaf halo behind the glyph. The deck is the whole console: the **field plaque** (expedition ledger, no photo by decision) + transport + a per-leg year-banded timeline (labels live inside the track); rapid steps buffer ~1.2s before settling; any jump renders trails AND journey lines as if watched through. The readout names the chapter, plus the **trip name only** — the recreation area lives on the plaque beside it. **A step taken while paused is a shortcut, not a scene**: `settleStep` passes `skipCeremony` so crossing a chapter line silently drops its journey line in and draws the trail, with no interstitial and no second blink (stepping while *playing* keeps the full ceremony — that's the film). Free exploration: docked field card on click, the **finder** (search-first trail list — replaced the register drawer; filters the list, never the map), and the **control rail** (filters/key/basemaps in one column so panels can't overlap); `nowT` still drives the reveal. Basemap wardrobe: Atlas (CARTO Voyager + Esri World Hillshade multiply, crisp to z16, under the parchment wash + CARTO labels tinted beneath it), Topo, Satellite — crossfaded. At rest the chrome recedes (register tucked, key folded, engraved wordmark). `?state=XX` deep-links; `?leg=N` (+`&cinema=1`) are headless-screenshot hooks. The reference mockup lives at `mockups/map-chapters.html`. **Efficiency (July 2026, measured):** a fixed session — load, 3 zoom steps in, a pan, 2 steps out — costs **674 tile requests, down from 1,916**. Three cuts got there, and none should be undone: predictive neighbour-zoom warming was **removed** (it speculatively fetched one level in AND one out on every rest — ~280 requests per zoom step where the screen needs ~90, half always discarded); `keepBuffer` went **8 → 2** (every retained tile is a live `<img>` the browser transforms on each frame of a zoom, so a big apron made zooming *heavier*); and the two full-screen `multiply` blends are dropped for the ~250 ms of a zoom via `.is-zooming` on the container. Accept the brief blur during a zoom — it is the network, not the code. Live multi-zoom flights and speculative warming have each now been tried and rejected; the honest fix would be vector tiles, which the no-build-step architecture rules out. **The film (July 2026):** the expedition opens on **one** combined slide (title + Chapter I) with the first shot framed *behind* the veil — it used to raise a title card, drop the veil onto wherever the map was last left, then raise a second card for Chapter I. `playFrom(ix, alreadyFramed)` carries that. Pen speed comes from `penDuration()`, measured in **on-screen pixels, not miles**: the camera frames every shot to fit, so a 12-mile hike can cover less screen than a tight 5-mile switchback loop (measured range 11–4,445 px), and the old miles-based timing gave a ~250× spread in apparent pen speed. Sub-linear (^0.62) so an epic breathes without the film dragging — total pen time stayed 169 s → 172 s. **Chrome & land (July 2026):** the whole-Atlas view uses `ATLAS_FRAME` — **pixel** padding, never a geographic `pad()`, because the floating chrome doesn't scale with the latitude span and Alaska stretching the bounds was pushing the southern hikes behind the deck (now 0 of 123 trailheads clipped, still at z4; the top pad is deliberately under the chrome's full height because the wordmark and rail sit in the corners). `stampVector` no longer trusts the opening/closing bisector alone — it scores directions around the trailhead against the WHOLE geometry and may extend its arm (`STAMP_RADII`), taking stamps sitting on ink from 21/113 to 0. Stamp fanning is measured in **screen pixels at the current zoom** and recomputed on zoomend (`refreshIconNudges`), because "too close" is a screen question: Savage Alpine and Savage River are ~1 km apart, one smudge at z11 and comfortably separate at z15. The deck is **fixed at 112 px in every mode** and 704 px wide, with the readout stacked over the transport (side by side they were one thin line against a plaque three times their height, wasting the right of the console; stacking cost no height because the plaque sets it, and saved ~180 px of width) — it used to bob between 84 and 142 px as trail names and modes changed. `.lg-name` reserves two lines whether or not it needs them AND centres the text in that box, so the common one-line case doesn't hang from the top over a gap; the field-log door sits beside the stats rather than under them, which is where the height came from. **Esri's terrain thins out in the far north:** past its coverage the server returns a tile that reads "Map data not yet available" rather than 404ing, so `tuneNorthernTiles` drops the native caps above 60°N (hillshade 16→15, topo 17→16 — measured: at Denali hillshade is real only to z15 and topo to z16, vs z16/z18 in the lower 48). Never raise those caps without re-measuring; the lower 48 keeps full resolution. |
| `scripts/atlas-chain.js` | **The Surveyor's Chain** — the map page's scrolling, time-proportional timeline (self-contained; map.js plugs in at `init`/`scrollToTime`/`scrollToHike`/`setReveal`/`forceOpen`/`setLocked`). Two invariants: every mark stays pickable, and the clock is derived from the ACTUAL laid-out dot positions so the line and the ink can't drift. **Spacing is trip membership only** (July 2026) — same-day outings used to be forced 30px apart so each could light on its own, which made one morning's two hikes sit *further* apart than hikes a day apart; now the reveal tells them apart by **order** (`hikeAtCenter` reports the mark under the playhead, which map.js's `scrubRevealToTime` prefers over any time search, because same-day siblings share a timestamp) and each dot's invisible hit halo shrinks to the room it has (`--hitx`). |
| `hike.html` + `scripts/hike-detail.js` | Single-hike page — **The Cartographer's Light Table** (rebuilt July 2026): one desk seen from above, holding the **slides** (left), the **sheet** (centre), the **paperwork** (right). Spec mockup: `mockups/hike-light-table-v2.html`. **The map never pans or zooms** — this page displays one GPX framed as large as the sheet allows; roaming belongs to map.html. Two stacked, identically-framed maps (topo clipped over satellite) are wiped by the **brass rail**, which the TOPO/AERIAL tab-pulls also drive; the framing pads for the rail's travel + knob so brass can never sit on the track. The sheet reads title block → **vitals band** (miles/gain/summit/grade as large engraved numerals, where a survey sheet carries them — viewpoints suppress it) → map → **acetate** → lower collar (provenance + scale bar). The acetate (`scripts/shape-of-day.js`) is bolted *under* the map so scrubbing the day's shape always happens with the trail in view; it walks a marker along both glasses. Its height is **fixed** (`ACETATE_PLOT_PX`), *not* AtlasShape's honest elevation-proportional scale — the map is the point of this page, and relative climb height is the home page's True Ascents. The gridline step adapts instead (`niceStep`): steep days step in 500s, flat ones in 5s. `⤢` gives the map the whole screen (Esc exits). Photos are **35mm slides**: a scrollable column, dim until hovered, full lightbox on click (the site's only true photo interaction) — each mount holds a "developing" sheen until Cloudinary delivers, which does *not* contradict config.js's no-placeholder rule (that governs the lightbox flip). Title-block lines auto-tighten onto one row (`fitOneLine`) and wrap at full size rather than squash. The timeline strip is deliberately **untouched** — its future is its own conversation. |
| `trip.html` + `scripts/trip.js` | One trip's chapter — **The Traverse** (rebuilt July 2026; `trip.html?tag=<trip_tag>`). A survey traverse is a connected series of measured legs between fixed points, which is exactly what a trip is. Built like map.html: **the land IS the page**, the instruments float on it, and the camera **cuts behind a veil, never flies** — but here it also **never roams** (drag/wheel/dblclick/box/keyboard all off, `cursor:default`); roaming belongs to map.html, and clicking a stop is the only thing that moves the camera. Four parts: the **frontispiece** (the curated opening photograph, full-bleed, lifting like the map's veil), the **mark** (chapter name engraved in the corner), the **sheet** (map.css's quick glance compacted to a standing card — collar → ruled vitals → slide-in-mount → wide `.atlas-door`; class names are the map's on purpose, one language not a fork), and the **traverse** itself. **The traverse**: every hike's real profile from `elevations.json` stitched end to end and normalised to the window (no sideways scrolling — seeing it all at once is the point); ground covered between stops **ramps** (driving sea level → 2,700 ft is elevation gained; drawing it flat left a wall at every trailhead); only walked ground is filled. **Hikes are stations, viewpoints are sightings** — a hike stands numbered ON the baseline, a viewpoint is a lettered dashed diamond that never touches it, floating on the travelled ramp. **The datum is labelled** (scale down the left, fill fading before the floor, hatched cut band beneath) because a bare baseline read as sea level when it was really 4,427 ft. **Level trips** (< 100 ft climbed all trip — only NY to LA so far) collapse the panel 204→126px into a survey rule and say so in words: a straight line must never read as "no climbing happened". Day bands **count viewpoints** (miles and feet don't). Camera clearances are **measured from the DOM** each cut, each panel cleared on its cheaper axis — never hardcode them, and never test `offsetParent` (these panels are positioned, so it reports null and silently collapses every pad). Hover repaints only the lit stop; **never rebuild the SVG on hover** — that destroys the node under the cursor, re-fires its own mouseenter, and eats clicks. **Refinements (July 2026):** the traverse is **scrubbed, not clicked station by station** — one hit rect spans the whole stage and the pointer is always at the NEAREST station, because the ground between two stops is a travel ramp belonging to no station, so every crossing used to drop the sheet back to the chapter card and pick the next one up again (walking the line made it flicker). The card is rebuilt only when the nearest answer actually changes, so a scrub costs one repaint per station crossed. Clicking commits to whatever the scrub is showing; **the land is now what pulls back** to the whole chapter. A **one-stop chapter** (`SOLO`, 3 trips) opens already standing on its stop and never deselects — the chapter card and the stop card described the same outing with the camera already framing it, so selecting it changed nothing visible and then offered a way "back" to a view you had never left. It must also stop **advertising** a choice it doesn't have, which is a separate job from having none: the trail ink and the pin are created `interactive: false` (dropping both the pointer cursor and the click), the traverse isn't scrubbed, and `.trav.is-solo` drops its cursor too. `SOLO` is therefore declared beside `LEVEL`, before any of the three is built, so nothing has to remember to opt out later. The sheet's single door **swaps direction** (`doorDirection()`): into a hike is forward, back onto the land wears `.is-return` with the chevron first and pointing left, as the hike page's doors are built. Hit rects are filled `transparent`, and that rule is deliberately **not** scoped to `.t-st` — the stage-wide rect defaulted to SVG's black fill and painted the whole panel out. The "Walk the traverse" hint is gone. |
| `echoes.html` + `scripts/echoes.js` | **Echoes of the Trail** (rebuilt July 2026): the Atlas in the present tense. Fresh Tracks (latest logbook entries), Trail Echoes (this month's anniversaries as scattered scrapbook snapshots; photo-less hikes become postmarked field notes), and **The Local Loop** — the Runyon Canyon shrine (tally wall, routes drawn to scale, roll-call, photo strip + in-site lightbox, Danny's own story text in the HTML — his words only). |
| `data/local-loop.json` + `data/local-loop/` | The Runyon Canyon record: 54 dated ascents with companions, both custom routes (simplified points + raw GPX archived), landmarks, photo IDs (`local-loop-##` on Cloudinary), links. **Deliberately outside hikes.json** so it never floods the Atlas's stats, maps, or crew tallies (Part V decision #4, resolved July 2026). |
| `crew.html` + `scripts/crew.js` | **Trail Crew — THE MUSTER ROLL** (rebuilt July 2026). A muster roll *is* a register, so the page is a bound volume open on a desk: left leaf the cover (tally + the core crew's photographs tucked in), right leaf the roll. **Crew is the Atlas's only PEOPLE page** — map/trip/hike are its three scales of place — so it deliberately does *not* borrow "the land is the page"; it borrows the language (parchment, engraved collars, `.atlas-door`). One lane per companion, **ordered by first signature, not by rank** (a ranked list hides the story: Shawna K. has 12 outings and 11 are 2022-23, Colm D. has 11 in 2026 alone), each outing a tick in its year ink. Three devices carry it: the ledger **brace + ditto** for a party who signed in on one outing (five did, at Astral Drive, March 2022 — Marcos L. signed the same *day* on a different trail and is correctly not braced); the **cross-read** (hover a lane and the outings shared with everyone else light across the other lanes, the triangulation idea delivered on the time axis); and the hollow **counter-lane** of days walked alone. Fixed columns, fluid track — every pixel of slack goes to the timeline. `?open=<name>` opens a lane. Viewpoints are counted here on purpose (crew tallies say "outings"). |
| `crew-member.html` + `scripts/crew-member.js` | **THE SERVICE RECORD** (`?name=Will%20R.`) — the same volume opened at one person's entry, reached by turning the page. Left leaf the record: the portrait in the cover's own mount (3:2 with Cloudinary's `g_face`; a 4:3 landscape put the person at about 8% of a frame captioned PLATE — PORTRAIT), a service block set as a **ledger of one-fact measurements**, their roll lane **enlarged**, and beneath it **the chapters panel**. Right leaf the country walked together as **numbered atlas plates** on the real Atlas basemap (Voyager + hillshade multiply + parchment wash, the same stack `map.js` wears) in year ink; the grid composes to the plate COUNT (`.plates.n1`…`.n5`), not to the desk, and one region gets a tall plate because that region is what its owner opened the page to look at. The 75 km complete-linkage clustering is kept wholesale from the old page; outings too far from the rest print as line art on a **loose sheet**. Plates never pan or zoom — roaming belongs to map.html. **Every companion has a record**, so every companion name anywhere in the Atlas leads to the same place. Both crew pages share `styles/crew.css`. **The enlarged lane is the plates' index, not decoration**: hover a mark and its trail lights on whichever plate holds it, and the plate that answered takes the ink in its own collar (`.plate.answering`) — one lit trail is a ~15px mark on a 536px plate and nothing used to say which sheet had spoken. Marks **stack when they collide** (screen px, recomputed on resize), because a buried mark cannot be pointed at and that would make some trails unreachable on the one leaf whose job is to reach them. **A VIEWPOINT DIMS BY ITS FILL** (August 2026): a viewpoint is a `CircleMarker`, not a polyline, and `weight`/`opacity` govern a circle's STROKE — so the dim style written for a line faded its white ring away and left the coloured fill at full strength, making a viewpoint look like the thing being pointed at at the exact moment every trail around it stepped back. **Three things landed August 2026, each with its long version in the file header.** **A TRIP IS ONE MARK.** Outings are grouped by `trip_tag` *before* anything is placed, so a chapter is one ringed mark carrying its own count, standing at the middle of its span (Will R.: 32 outings, 14 marks). Pixel collision used to decide what fused, which could split one chapter across a ringed 3, another ringed 3 and two strays with nothing saying they were the same week; it survives only as a backstop for a run of standalone day hikes, and such a mark lists every outing rather than opening one arbitrarily. Clearance is measured **edge to edge**, so `dia()` in the JS must stay in step with the 11px/16px diameters in `crew.css`. **THE LANE WRAPS.** Five years to a **course**, so a year is one fifth of the leaf on every line forever and the Atlas's sixth year starts a second course instead of squeezing the first — nothing is re-tuned as the years arrive, and wrapping hands every year back the room it had in 2022. A partial final course draws only the fraction its years need, exactly as the last line of a paragraph is short. `?demo=<years>` rehearses a longer Atlas and **ships**, for the same reason the film keeps `?q=` and the map keeps `?leg=`: a thing you cannot look at is a thing nobody checks. It touches the lane and nothing else, may only lengthen the Atlas, and prints "rehearsal · N years" in the lane header so no invented outing can be mistaken for one that happened. **THE CHAPTERS PANEL.** The old one-line readout grew into the space under the lane and carries three states in one box: at rest the legend plus every chapter walked together; on a single outing its name and place with that list still standing; on a chapter's mark its span and every outing on it, each one a door. **It reserves its tallest state's height** (`reservePanel()` measures every state this record can reach, at the current width) so nothing moves between them, and it is **static** — a sticky version rode over the courses of a wrapped lane and clipped them. **It never rebuilds itself under the cursor OR under the keyboard**: hovering a row lights ink and nothing else (a repaint destroys the node under the pointer, re-fires its own mouseenter and eats the click — trip.js has the scars), and `setPanel` refuses to repaint while focus is inside it. **Leaving a mark is not leaving the panel** — a mark's mouseleave only *schedules* the return to rest (340 ms) and entering the panel cancels it, or the list vanishes as you reach for it. **The panel scrolls itself into view, and the pointer is PINNED while it does** (`revealPanel`): the record is taller than its leaf, so at most scroll positions the reply lands below the fold at the moment it fills — the leaf therefore moves the least it can to stand one row clear of the door. That drags the mark out from under a cursor that never moved, firing the same mouseleave as walking away, so a leaf we scrolled ourselves suspends the return to rest until the pointer genuinely travels ≥3 px. Genuinely is the word doing the work: a mouse crossing into an element emits mouseenter and *then* a pointermove, so the move that delivers the hover always arrives a fraction of a pixel later and unpinned it instantly. The pin's reference point is set by that first move, not by the pin. The door is `position: sticky` and on a short record travels with the scroll, eating the clearance being made for it, so the reveal **measures a second time** once the leaf stops rather than reimplementing sticky from the outside. **The pointer opens the panel; the keyboard only lights the ink**, because the panel sits after all 14 marks in the document, so if every mark repainted on focus the chapter list was gone by the time you tabbed to it. This **replaced the ledger's Trips row**, the one deletion that removed something working: six links of the same green separated by `<br>`, so a wrapped chapter name read as two chapters, in a ledger where every other row is one short measurement. **THE SIGNATURE NUMBER IS crew.js's, TO THE TIE-BREAK.** A debut outing can carry a whole party (five signed in together at Astral Drive, March 2022), so `compareHikesChrono` alone returns 0 and the sort falls back to insertion order — `hiked_with`, stored alphabetically — while the roll breaks the same tie by outings, most first. Six of twenty companions disagreed, and Will R. was No. 11 on the roll and No. 12 on his own record. In a bound register an entry number is written once, in ink: **if the roll's ordering ever changes, change it here in the same commit.** |
| `scripts/crew-book.js` | **The page turn** — the one gesture carrying a visitor between the two leaves of the crew volume, defined once so the halves can't drift. It is a single turn *filmed across two documents*: the page being left swings a leaf shut over itself (`bookTurnTo`), the page arrived at swings the same leaf open again (`bookOpen`). Navigation fires while the paper lies flat, so the document swap happens behind it — the book turns, it never blinks. It is a **panel, not the leaf itself**: the record's right leaf holds live Leaflet maps, and a rotated container makes `getBoundingClientRect` report the transformed box, which would quietly wreck every `fitBounds`. The easing is load-bearing — a page is steeply foreshortened for most of its arc, so both curves spend their time where the page can actually be seen. Mark any crossing with `data-turn-to` and `bookWireTurns()` finds it. Honours `prefers-reduced-motion` by navigating straight through. |
| `credits.html` | "The Overlook": hero slideshow + asset credits. Personal statement arrives in Phase 4. |
| `scripts/trail-renderer.js` | Shared renderer for both maps (`isInteractive` flag), plus `formatHikeText()` for `**bold**`/newline rendering. |
| `scripts/nav-updater.js` | Highlights the active nav link; powers the credits slideshow. |
| `styles/` | `base.css` (the earthy palette as CSS custom properties + nav + footer + **the Atlas doors**: the shared `.atlas-door` component behind *every* crossing between the map, a hike page and a trip page — see "The doors" below. One brass-on-cream frame, a glyph naming the DESTINATION (open field log / folded map / surveyed section line) and a chevron-through-a-doorway pointing the way; `.is-return` mirrors both for the trip back. Only sizes are set per page — never restyle one door alone; the door's `prefers-reduced-motion` rule lives here too, for exactly that reason — the copy that used to live in `crew.css` was honoured on the two crew pages while the same door went on lifting and sliding everywhere else. **There is no global `box-sizing: border-box` in this codebase**, so never give a padded door `width: 100%`; `display:flex` already fills its container, and the width would push it past its neighbour. One scoped exception: `crew.css:52` switches border-box on for `.crew-book` and everything inside it, because the book is laid out as nested full-height boxes — check which side of that line an element is on before reasoning about widths) and one sheet per page or section: `keymap.css` (the volume, the leaf, and the shared plate collar), `home.css` (hero film + Odometer), `threads.css`, `observatory.css`, `records.css`, `map.css`, `hike.css`, `echoes.css`, `crew.css` (both leaves of the crew volume, and **the only sheet that hides the shared footer**: the book is `position: fixed` and inset from every edge, so nothing in flow can push a static `#333` footer below the fold — it printed a dark band above the spread and a slab down each side, with four focusable links nobody could see. Don't "restore" it without unfixing the book first), `trip.css`, `credits.css`. No inline CSS anywhere except `404.html` (self-contained by design). |
| `data/intro-film.json` | **The film record — generated by `tools/render-intro.py`, don't hand-edit, but DO commit it.** The handful of facts that are true of the *baked video* and can therefore never be re-derived from today's data: `zSpan` (the zoom ramp the flight was cut with), `atlasStart` (the ground its last frame shows) and `inked` (the trail ids it actually draws). This is what lets the live half keep growing while the first half is a finished picture. Without it, two things break silently: a new Angeles hike would clear the flight's gate and the sheet would open carrying a trail the video never drew; and a new hike outside the current bounds would widen the landing frame, which changes the ramp's span, which changes the zoom at **every moment of the flight** — a flight that is now a fixed video. (That second one bit twice during the build.) `--record-only` rewrites it from a live boot without baking, which is only valid when nothing touching the flight has changed. |
| `data/hikes.json` | **The single source of truth.** All hike data. |
| `data/almanac.json` | **Generated — don't hand-edit.** The Hike Almanac, baked: per hike, sunrise/sunset (local wall clock), the temperature and conditions at each, the day's apex, the UTC offset, and `on_trail` (the boots-on/boots-off window in UTC, or null). 123 rows, 33 KB, written by `tools/build-almanac.py`. **Weather on a day in the past never changes**, so it is derived data like elevations.json — fetching it live cost 5–10 s per visit, in which the page had already landed without the card. The page falls back to the live Open-Meteo call for a hike not yet baked, so a new record still works before the tool is re-run. `on_trail` is baked too so the card owes **nothing to the GPX**: the track is the biggest file the hike page loads (several over 400 KB) and a small weather card must not wait on it. |
| `data/crew.json` | Trail Crew portrait registry: `{"Will R.": "crew-will-r"}` — hand-picked Cloudinary photos of core-crew members (face-aware `g_auto` crops). **Missing: Robby G. and Mila R.**, who are core crew and show as brass initial plates on the book's cover. The ritual: Danny drops `Will R.jpg` into `intake/`, Claude uploads it (public ID `crew-will-r`, Media Library folder `trailprint-atlas/trail-crew`) and adds the entry. Missing entries fall back to a landscape from a shared hike. |
| `data/trails/*.gpx` | Raw GPX tracks, one per hike — the archival record. Never served to visitors. |
| `data/trails.geojson` | **Generated — don't hand-edit.** All trails, simplified, in one file for the map page. Rebuild with `python3 tools/build-trails.py` whenever a GPX is added or changed. |
| `data/elevations.json` | **Generated — don't hand-edit.** Per-hike elevation profiles (120 integer feet values, distance-sampled, keyed by trail_id), written by build-trails.py from the USGS-corrected ground truth. Feeds the homepage's True Ascents panorama AND the hike page's acetate; any feature needing a climb's shape reads this, never raw GPX. |
| `data/ground-elevations.json` | **Generated — don't hand-edit.** The USGS 3DEP correction cache: per-trail corrected profile + true high point + its coordinates. Committed so rebuilds never need the network, but **not served to visitors** — elevations.json is what ships. |
| `tools/correct-elevations.py` | **The elevation ground-truther** (July 2026). A GPX's coordinates are accurate; its `<ele>` is GPS altitude and reads 15–130 ft high, which is why summit numbers never matched the elevation graph. This asks USGS 3DEP (10 m) what the ground truly is at each recorded point and writes ground-elevations.json. Benchmarked within 1–3 ft of published heights wherever a track crosses the summit; it independently confirmed 3 good records and caught 4 wrong ones. Falls back to the global `mapzen` model outside US coverage (3DEP reaches Vancouver, not Whistler). Incremental — only queries trails it hasn't seen; `--force` re-queries, `--only tta_34` targets. Run it after adding a hike, before build-trails.py. |
| `tools/build-countries.py` + `assets/countries.json` | **The national silhouettes** (July 2026). The US map asset has every state; nothing had Canada. This fetches world country geometry, keeps only the countries present in hikes.json, projects each in **Lambert azimuthal equal-area centred on its own middle** (Web Mercator would stretch a high-latitude country badly, and Canada was the first customer), simplifies with Douglas-Peucker, drops islets under ~3.5% of the box, and writes one normalised SVG path per country. **Generated — don't hand-edit**; re-run it after a hike in a new country (`--force` rebuilds all). Canada: 30 rings → 17, 682 → 518 points, 6.6 KB. It also writes **`assets/atlas-frame.json`** (August 2026): the same countries through ONE shared LAEA centred on their combined middle, for the home page's index diagram — one projection, so the dots and the coastlines agree. |
| `tools/check-track-clock.py` | **The clock truth-teller** (July 2026). A GPX carries two independent things, where you went and when; the route is reliable, the clock is not always. Some apps **thin the recording on export and re-stamp the survivors on a synthetic cadence** — the shape survives, the timing does not. Measured: AllTrails puts its points 3.5–3.7 m apart and peaks near 4 mph; onX's **web** export put them 5.3–7.8 m apart and peaked at 8–20 mph, printing "1h 08m" on a 7.2-mile day. Two tests, because either alone misses a case: whole-track average (>4.0 mph) and 95th-percentile step (>6.0 mph — catches a track whose total looks innocent). Run it on any GPX or the whole logbook. **This file owns the thresholds**; `build-almanac.py` imports its `assess()` and `shape-of-day.js` mirrors it for the fallback path — retune here, not in three places. |
| `tools/build-almanac.py` | **The weather baker** (July 2026) → `data/almanac.json`. One Open-Meteo archive call per hike, condensed to the six values the card shows plus the on-trail window. Incremental; `--force` re-fetches, `--only tta_123` targets. Requests **two** days, not one, because above the Arctic-ish latitudes the Atlas now reaches a midsummer sun sets *after midnight* (Savage Alpine's is stamped 00:21 the next day) and a single day's hourly array has no hour for it to point at — hours are matched by **timestamp**, never by hour arithmetic. Uses `curl`, not urllib, for the same reason correct-elevations.py does: the system Python on macOS has no usable certificate bundle. Run it after adding a hike, alongside build-trails.py. |
| `tools/build-trails.py` | The GPX → trails.geojson + elevations.json build script (stdlib-only Python; runs locally, never ships). Prefers ground-elevations.json for profiles and names any trail still on raw GPS altitude. Warns if hikes.json and data/trails/ disagree. |
| `tools/render-intro.py` + `tools/upload-intro.py` | **The film pipeline** (July 2026). `render-intro.py` bakes the satellite half to video by driving the cutting room with `?live=1` — it never owns a copy of the choreography, it only calls `warm`/`quiet`/`settle`/`dress` on `AtlasFilm`. Self-bootstrapping venv at `tools/.render-venv`. `--quality final --dsf 1.5 --mblur 3` is the ship setting (2880x1620, 30 fps, ~20 min, ~47 MB); a full bake also writes `data/intro-film.json`, a partial one (`--from/--to`) deliberately does not. `upload-intro.py` puts the master on Cloudinary once (`atlas-intro-film`) and the page asks for `f_auto:video,q_auto,w_<1280|1920|2560>,c_limit` — measured 5.40 / 6.77 / 14.61 MB, which is why the page never goes past 1920 for device-pixel-ratio alone. **Re-render after ANY change to the flight or to `atlasEnd`.** |
| `tools/new-hike.py` | **The hike-entry pipeline** (Phase 3). Drop a GPX + photos into `intake/` (git-ignored), launch the wizard — double-click `New Hike.command`, or VS Code: Terminal menu → Run Task → "New Hike" (tasks in `.vscode/tasks.json`), or `python3 tools/new-hike.py` — and answer it. It derives what it can from the GPX and pre-fills the rest from the Atlas's own history (Phase 3.2): suggests the trail name from the GPX's embedded title, detects repeats by trailhead proximity/name and defaults last time's answers (miles/elevation still default to *this* GPX's estimate — outings differ), offers location/region used by previous hikes near the coordinates so spellings never drift, and asks the trip question before hike_type so a hike joining an existing trip defaults to that trip's established style (keeping a trip's map icons consistent). Then it writes the record at the top of hikes.json (textual splice — existing records stay byte-identical), files the GPX, uploads photos to Cloudinary — flat public IDs, auto-filed into the Media Library's `trailprint-atlas/tta_NN-trail-name` folder per hike (needs `tools/cloudinary-credentials.json`, git-ignored; copy the `.example` file) — rebuilds trails.geojson, and verifies everything. `--dry-run` rehearses without writing. Leaves description/flora/fauna empty — Claude drafts those in-session. Supports GPX-less viewpoint entries. **Asks for the trip star** when a hike joins a trip that has none, and registers it in `config.js`'s `TRIP_STARS` — the one place the wizard edits a source file rather than data, and it edits it as TEXT (like the hikes.json splice) because re-serialising code would reformat far more than the line being added. |
| `assets/` | Icons, the US map SVG, the timeline mountainscape. |
| `docs/` | The roadmap (`STATE_OF_THE_ATLAS.md`) and historical documents (original PRD). |

## The cross-lit spread — the home page's one law

The home page is a **volume lying open**. The left leaf is the land; the right
leaf is the front matter. Established August 2026, and it has three parts, all
of which are easy to break by accident:

1. **You act on the right; the land answers on the left.** Nothing on the left
   leaf is hoverable or clickable except the door out. This is Danny's call, and
   it also deletes a whole class of bug — there is no cursor on the left to
   referee against the reader's scroll position.
2. **The camera never moves on its own.** It cuts behind a veil, it never flies,
   and it **holds the last subject it was given**. `AtlasKeyMap.clear()` is a
   deliberate no-op. Sending the plate home when the pointer leaves made the
   leaf slam in and out of California between every hike.
3. **Nothing floats, and nothing follows the cursor.** Every panel answers on
   the leaf, or in a fixed readout of its own. The Observatory's shared tooltip
   and the Threads hover card are both gone; a native `title` attribute counts
   too, and was removed from the Territories tiles for the same reason.

Nine places light the leaf today: a milestone row, an Effort Field dot, a
territory, a True Ascents ridge, a Cadence month, a Network station, a specimen
drawer, a record crown, an expedition mount. Adding a tenth means calling
`AtlasKeyMap.light(ids, label, place)` — never touching Leaflet, which only
`keymap.js` may do.

## The doors — how the three map-shaped pages open into each other

The Atlas has three scales of place: **the land** (map.html — everywhere, all at
once), **the chapter** (trip.html — one trip, end to end), and **the day**
(hike.html — one outing, in full). Every crossing between them is the same
`.atlas-door` component, so moving between scales always looks like the same
gesture. Established July 2026 — add a new crossing to this table, don't invent
a fourth kind of link.

| From | To | Where it lives |
|---|---|---|
| **Land** → day | `hike.html?id=` | the deck plaque's compact door; the map sheet's wide `.ms-bridge` |
| **Land** → chapter | `trip.html?tag=` | the map sheet's `.ms-chapter` door (compact, under the field-log door — the log is why you opened the sheet, the chapter is the wider frame); **and the chapter name in the deck readout**, which is a link |
| **Day** → land | `map.html?sheet=` / `?restore=land` | the hike page's `.land-door` |
| **Day** → chapter | `trip.html?tag=` | the hike page's `.chapter-door`, sitting beside `.land-door` as a matched pair of ways out. Hidden on a standalone outing. The collar's `c-trip` line **names** the chapter but is deliberately NOT a link — two routes to one page inches apart is the redundancy this table exists to prevent |
| **Chapter** → day | `hike.html?id=` | the trip sheet's wide door, whenever a stop is selected |
| **Chapter** → land | `map.html?trip=` | the trip sheet's wide door at rest |

### The fourth scale: the people, and the doors into the book

Trail Crew is the Atlas's one **people** page, not a fourth scale of place, and
it has its own pair of crossings — the same `.atlas-door` component, so the
gesture still reads as the Atlas's, but they open a *book* rather than a
*view*. Added July 2026; the same rule applies, add a crossing here rather
than inventing a new kind of link.

| From | To | Where it lives |
|---|---|---|
| **Roll** → record | `crew-member.html?name=` | the wide door at the foot of an opened lane's drawer, **and** every photograph on the cover |
| **Record** → roll | `crew.html?open=<name>` | the record's `.is-return` door, which lands on that person's own line, opened, rather than at the top of the book |
| **Record** → day | `hike.html?id=` | every mark on the enlarged lane, **every row of the chapters panel while a chapter is open**, every trail on a plate, every trailprint on the loose sheet |
| **Record** → chapter | `trip.html?tag=` | **the chapters panel**, in all three of its states: the rest list of every chapter walked together, the chapter's own name at the head of an opened chapter (`.readout .t.tlink`), and the lane's ringed chapter marks, whose `href` is the trip rather than a hike. The service block's **Trips row that used to carry this is gone** (August 2026) — a list of links has no business in a ledger of one-fact measurements, and the chapters now stand where the timeline is already pointing. NB **a record only holds the outings walked *together*, so the panel says which it is counting**: Lisa R. shares 2 of the Summer 2024 East Coast Trip's 3, and the chapter door beside the list opens a page showing all 3, hence "2 of the chapter's 3 outings" under the heading "Walked together on this chapter" rather than "Every outing on this chapter" |
| **Roll** → day / chapter | `hike.html?id=` / `trip.html?tag=` | inside an opened lane's drawer |
| **anywhere** → record | `crew-member.html?name=` | a companion's name on a hike page, the Runyon roll-call chips on Echoes, and **every station in the home page's Triangulation Network** — **every companion has a record**, so a name never leads to a generic front door. The one exception is a Runyon regular with no `hikes.json` entry at all (the Local Loop is deliberately outside it), who still gets `crew.html` rather than an unsigned page |

Both directions between roll and record run through **the page turn**
(`crew-book.js`), which is the whole point of the pair: a person is not a
separate page, they are a page *in* the same volume.

`map.html?trip=<trip_tag>` is the return path, and it lands you **inside** the
chapter rather than at the whole Atlas: it parks on the chapter's last leg (so
every trail of the trip is already inked), frames the whole chapter, and stays
**paused** — a place to stand, not a film to start. An unknown tag falls back to
the ordinary whole-Atlas view.

One wrinkle worth knowing: **a map chapter and a trip are not the same thing.**
Distance decides chapters (`SORTIE_KM`), so a local overnight like Mount Lowe is
filed under the home stretch even though it is a real trip. The deck therefore
has two phrasings — `Chapter XXXVI · Alaska Camping Trip` when the chapter *is*
the trip, and `Chapter XIV · part of Mount Lowe Backpacking Trip` when the trip
sits inside a home stretch. Both link to the trip page; neither misreports the
chapter.

## Data

### Adding a hike (the Phase 3 pipeline)

New hikes enter through `tools/new-hike.py` — see its file-map row above. The ritual:
Danny drops the AllTrails GPX export + photos into `intake/` → launches the wizard
(double-click `New Hike.command`, or the "New Hike" VS Code task) → mostly presses Enter
(miles/elevation default to the GPX estimate; he can type AllTrails' numbers instead) →
Claude drafts `description`/`flora`/`fauna` for review → check the page in Live Server → commit.
Records land in the exact format below; the wizard leaves the three prose fields empty,
so a record with an empty `description` means "drafting still owed."

### `hikes.json` schema (123 records as of July 2026)

Every page derives from this file. **Never hardcode hike data anywhere else.** When a feature
needs new per-hike information, the first step is always to add the field to the schema.

| Field | Type | Notes |
|---|---|---|
| `trail_id` | string | `"tta_NN"`, sequential. Used in URLs (`hike.html?id=tta_43`). |
| `trail_name` | string | Repeat hikes share the same name — grouping key for "unique trails". |
| `date_completed` | string | `"YYYY-MM-DD"`. **Always read in UTC** (see conventions). |
| `location` | string | Park / natural area (e.g. "Joshua Tree National Park"). |
| `region` | string | `"City, ST"` — the nearest town and its state/province abbreviation. Detail only: it no longer decides where a hike is *collected* (see `country`). |
| `country` | string | On every record. **The Atlas collects the US state by state and everywhere else one whole COUNTRY at a time** (decided July 2026) — there will always be far fewer hikes abroad, and a lone province tile beside fifty states reads as an accident. It's a real field because it can't be derived: `"Dublin, ??"` has no abbreviation a suffix rule could recognise. Never read it raw — use `territoryKey()`/`territoryName()`/`isUsState()`/`hikeCountry()` from atlas-data.js. The wizard infers it from the region where the suffix answers it, and asks where it doesn't. |
| `primary_geography` | string | One of: Chaparral, Coastal, Coastal Chaparral, Desert, Mountain Forest, Riparian Canyon, Riparian Forest, Riparian Meadow, Tundra, Urban Edge. Drives hike-hero color. Colors live in `BIOME_COLORS` (observatory.js); the wizard's list is `GEOGRAPHIES` in new-hike.py — a new kind of country must be added to both. |
| `miles` | number | Trail distance. |
| `elevation_gain` | number | Feet. |
| `summit_trail` | bool | A real summit only. Scenic overlooks that top out low are **not** summits (Baldwin Hills at 427 ft and the Griffith Observatory loop at 1,139 ft were demoted July 2026); Scout Lookout, a genuine climb, stays. |
| `summit_elevation` | number\|null | **Derived, never typed.** The USGS-corrected height of the highest ground actually walked, from `tools/correct-elevations.py`. Decided July 2026: the Atlas records the boots, so a loop that tops out 17 ft below Mount Waterman's true summit says 8,020, not 8,038 — the peak's own height is context, carried by `peak_name`. The wizard no longer asks. |
| `peak_name` | string\|null | The mountain's name, which is often **not** the trail's ("Waterman Mountain Loop Trail" tops out on Mount Waterman). Asked by the wizard when `summit_trail` is true; `null` for unnamed high points (Savage Alpine tops out on an unnamed ridge, and is still a real summit) and for every non-summit. Never read it raw — see the three naming helpers under Code conventions. |
| `difficulty` | string | Easy / Medium / Hard. |
| `hike_type` | string | Day Hike / Backpacking / Day Trip / Overnight Trip / Car Camping / Viewpoint. Drives map icons. **Describes the outing style — essentially how you slept — not the individual walk** (decided June 2026). Pick by: slept on the trail → Backpacking; tent/campground → Car Camping; lodging (hotel/Airbnb) → Overnight Trip; home the same day, no overnight → Day Trip; standalone outing from home → Day Hike; a scenic stop that isn't a real hike → Viewpoint (overrides the rest). So a relaxed day hike taken *during* a lodging trip is **Overnight Trip**, not Day Hike. This is independent of `trip_tag` (icon category vs. specific-trip grouping): every hike on a given trip shares one core type, and it's the norm for a trip's members to all carry it. |
| `hike_size` | string | Solo / Duo / Group. |
| `hiked_with` | string[] | Names as `"First L."` (e.g. "Luke R."). Empty for solo. |
| `description` | string | AI-drafted field-guide text. May contain `**bold**` — render via `formatHikeText()`. |
| `flora`, `fauna` | string | One species spotlight each, "Name (Latin) — fact" format. |
| `notes` | string\|null | **Danny's journal — currently null on every record.** Phase 4 fills it. His words only. |
| `fire_memorial` | object | *Absent on most records — always guard.* `{ "fire": "Eaton Fire", "date": "YYYY-MM-DD" }` on trails that burned **after** the hike (5 records: Eaton/Palisades/Bridge fires, verified against real perimeters July 2026). Drives the hike page's muted memorial banner ("Hiked six weeks before the Palisades Fire…"). Hand-added after research — not a wizard question; edge-singed trails (Dawn Mine) get a factual sentence in `description` instead. No map mark, by design. |
| `trip_tag` | string\|null | Groups multi-day trips: `"Trip Name - Mon YYYY"`. Must match exactly across a trip's hikes. |
| `all_trails_url`, `official_trail_url` | string\|null | External links. |
| `latitude`, `longitude` | number | Trailhead coords — used for dots, viewpoint markers, weather. |
| `gpx_file` | string\|null | Filename in `data/trails/`. Null for viewpoints / missing tracks. |
| `recorded_times` | object | *Absent on all but one record — always guard.* `{ "start": "YYYY-MM-DDTHH:MM:SSZ", "end": "..." }`, **UTC**. The almanac's "On the Trail" clock normally rides on the GPX's own per-point timestamps; this field supplies it when a track has been **replaced** by a timeless AllTrails route download and the day's real hours are still known. Wins over the GPX when present. So far only `tta_88`, whose recording paused mid-hike and drew a false line on the return, so the route was re-downloaded for accuracy while the genuine 9:33 AM–12:05 PM window was preserved here (recovered from the original recording in git history). Add it by hand, like `fire_memorial` — not a wizard question. |
| `images` | string[] | Cloudinary public IDs (no URL, no extension). |
| `videos` | string[]\|null | YouTube URLs. *Field absent on older records* — always guard. |

### Naming conventions

- GPX files: `Trail_Name_MM.DD.YY.gpx`
- Cloudinary image IDs: `tta_NN-trail-name-##` (kebab-case trail name, 2-digit photo index)

## Code conventions

- **Viewpoints are not hikes** (decided July 2026). Any user-facing "hikes" tally excludes
  `hike_type: "Viewpoint"` — use `isViewpoint()` from atlas-data.js, never re-test the string.
  Where viewpoints appear they're named as such (map wordmark/deck, Territories tiles, trip
  stats); counts that legitimately mix both say **"outings"** (crew tallies, echoes). Crew
  tallies deliberately keep viewpoints — excluding them would demote real companions below
  the core-crew threshold (Rachel G. sits at exactly 10 shared outings).
- **Name a summit through atlas-data.js, never by massaging `trail_name`.** A regex that
  stripped "via…"/"Trail"/"Loop" used to guess peak names in observatory.js; it now lives
  inside one helper, not as a pattern to copy. Which helper depends on what the label is
  doing (decided July 2026): `summitLabel()` when **naming the summit itself** — an unnamed
  high point reads **"High Point"**, never a trail name dressed up as a mountain;
  `climbName()` when **telling climbs apart** (True Ascents ridge labels, milestone notes),
  where the trail name is the useful fallback; `peakName()` for the strict answer, or null.
  A summit with no named peak is still a summit — `summit_trail` stays true.
- **File a hike's place through atlas-data.js, never by splitting `region` yourself.**
  `territoryKey()` returns the identity a hike is collected under — a US state
  abbreviation, or a whole country — and `territoryName()`, `isUsState()` and
  `hikeCountry()` answer the rest. Three separate copies of a US-state list used to
  drift around the codebase (observatory.js, threads.js, map.js's `zoomToState`);
  there is now one, and both `?state=XX` and `?country=Name` deep-link the map.
- **Companion names are stored alphabetically** in `hiked_with` (decided July 2026), sorted
  by the wizard on entry. Pages render them in file order — don't re-sort at display time.
- **UTC dates, always.** `date_completed` parses as UTC midnight; reading it with local-time
  getters shifts hikes a day for anyone west of UTC. Use `getUTCFullYear()` / `getUTCMonth()`
  etc. — never the local variants — when deriving year/month/day from hike dates.
- **Sort hikes with `compareHikesChrono` / `compareHikesChronoDesc`** (atlas-data.js) — never
  by `date_completed` alone. Multi-hike trip days share a date; the comparators break ties by
  tta number (assigned in the order hiked), so same-day hikes display in the order they
  happened instead of file order (which is newest-first, i.e. backwards).
- **Long-form text** (`description`, `notes`, `flora`, `fauna`) renders through
  `formatHikeText()` in `trail-renderer.js` — it converts `**bold**` → `<strong>` and
  newlines → `<br>`.
- **Year colors and hike-type icons** come from `ATLAS_CONFIG` in `config.js`. The year
  inks are GENERATED by `atlasYearInk()` (July 2026 "Blaze Rose" palette: golden-angle
  wheel, parchment-adjacent yellows banned, back-to-back years always ≥115° apart) —
  `COLOR_MAP` builds itself 2022 → current year, so a new year needs no edit. Never
  hand-add a year color; retune the formula instead. Icons: `STAMPS`/`atlasStampSvg()`
  on the map page, `ICON_MAP` PNGs on the older pages. Don't define color/icon maps
  elsewhere.
- Comments explain the *why*, not the *what*. `const` by default, `async/await` for fetches.
- Phase 1's structural debts are all retired (five nav copies, hardcoded Cloudinary name,
  per-page data/date logic in 1.1–1.4; inline CSS extracted to `styles/` in 1.5). New CSS
  goes in the page's stylesheet — or `base.css` if genuinely shared — never inline. Reuse
  the palette variables (`--trail-green`, `--evergreen`, `--charcoal`, …) from `base.css`
  instead of hardcoding hex values.

## Two engines — don't grow the parity debt

**The whole Atlas has only ever been checked in one engine at a time.** Danny develops
in Zen (Gecko); the headless harness drives Chrome (Blink); the two have already
visibly disagreed (crew.html scrolled in one and not the other). The full sweep is its
own roadmap item — **Part F, deliberately scheduled after the design overhaul** — but
the debt is easy to grow by accident, so new work carries these rules now. The point is
that F stays the size it already is.

- **Don't add another bare `100vh` shell.** Eight of the nine stylesheets already build
  a fixed-viewport shell (`100vh` and/or `position: fixed` + `overflow: hidden`), **none
  declares a `min-height`**, and nothing uses `dvh`/`svh`/`lvh`. So there is no floor at
  which a short window stops clipping and starts scrolling. A new shell needs that floor
  from the start; retrofitting nine of them is the work F exists to do once.
- **Anything that can overflow needs an explicit internal scroller with a visible
  affordance.** In a fixed shell the page never scrolls as a whole, so reaching hidden
  content depends on where the cursor is — and **scroll chaining and overlay-scrollbar
  visibility are exactly where Gecko and Blink differ.** A scroller nobody can find is a
  bug in one engine and invisible in the other.
- **Prefer what both engines have shipped, and say so in a comment when you deliberately
  skip something.** `scrollend` is avoided in `crew-member.js` for this reason (a timer
  does the job). `navigator.connection` does not exist in Firefox at all, which is half
  of why `intro-film.js` reads its width from the layout instead.
- **The harness cannot see Gecko.** When a change touches scrolling, sticky positioning,
  or a fixed shell, hand Danny a specific thing to check in Zen — which page, which
  element, what to try. Verifying in Chrome alone is not verifying.

## History

GEMINI.md (the previous collaboration charter) was replaced by this file in June 2026 —
its philosophy survives above; its tech claims and roadmap were stale. The original PRD
lives in `docs/` as a historical record. The roadmap that governs current work is
**docs/STATE_OF_THE_ATLAS.md** (Phase 0 completed June 2026).
